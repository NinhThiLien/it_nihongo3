<% provide(:title, "Home") %>

<div class="products-catagories-area ">
  <div class="amado-pro-catagory ">
    <div class="row">
      <% @books.each do |book| %>
        <div class="single-products-catagory ">
          <%= link_to book do %>
            <% if book.picture.present? %>
              <%= cl_image_tag(book.picture, {:style => "height:450px;"}) %>
            <% else %>
              <%= image_tag("default_book.jpg", {:style => "height:450px;"}) %>
            <% end %>
            <div class="hover-content">
              <div class="line"></div>
              <p><%= book.author %></p>
              <h4><%= book.title %></h4>
            </div>
          <% end %>
        </div>
      <% end %>
      
      <div class="digg_pagination">
				<%= will_paginate @books %>
			</div>
    </div>
  </div>
</div>
